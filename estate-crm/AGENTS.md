# Priv CRM

Multi-tenant CRM for real estate brokerage organizations.

@CONTEXT.md

## Commands

We are using `bun` as runtime and package manager. Use `bun add`, `bun run`, `bunx` commands and other standard `bun` commands. Do not use `npm` or `yarn` or any other package manager.