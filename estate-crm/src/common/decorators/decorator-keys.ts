/** Metadata key for role-based access control */
export const ROLES_KEY = 'roles';

/** Metadata key for marking routes as public (no auth required) */
export const PUBLIC_KEY = 'public';

/** Metadata key for tenant context */
export const TENANT_KEY = 'tenant';

/** Metadata key for bypassing ownership checks */
export const BYPASS_OWNERSHIP_KEY = 'bypass_ownership';